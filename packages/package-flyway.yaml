---
package:
  name: flyway
  version: 11.8.2
  epoch: 0
  description: Flyway database migration tool
  copyright:
    - license: Apache-2.0

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - openjdk-17-jre

pipeline:
  - uses: fetch
    with:
      uri: https://repo1.maven.org/maven2/org/flywaydb/flyway-commandline/${{package.version}}/flyway-commandline-${{package.version}}.tar.gz
      expected-sha256: b01677117b6be97e669314cc502da85bdce20b426bafc291738368ab980c8ca5

  - name: Install Flyway
    runs: |
      set -e
      FLYWAY_VERSION="${{package.version}}"
      FLYWAY_VERSIONED_DIR="/opt/flyway-${FLYWAY_VERSION}"
      
      # Create versioned installation directory
      mkdir -p "${{targets.destdir}}${FLYWAY_VERSIONED_DIR}"
      
      # Extract Flyway
      tar -xzf "flyway-commandline-${FLYWAY_VERSION}.tar.gz" \
        -C "${{targets.destdir}}${FLYWAY_VERSIONED_DIR}" \
        --strip-components=1
      
      # Make flyway executable
      chmod +x "${{targets.destdir}}${FLYWAY_VERSIONED_DIR}/flyway"
      
      # Create generic symlink for easier version management
      ln -s "${FLYWAY_VERSIONED_DIR}" "${{targets.destdir}}/opt/flyway"
      
      # Create symlink for easy access
      mkdir -p "${{targets.destdir}}/usr/local/bin"
      ln -s "/opt/flyway/flyway" \
        "${{targets.destdir}}/usr/local/bin/flyway"

test:
  pipeline:
    - runs: |
        flyway --version

update:
  enabled: true
  github:
    identifier: flyway/flyway
    strip-prefix: flyway-
    tag-filter-prefix: flyway-
